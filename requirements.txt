# Fantasy F1 2026 — Python Dependencies
# Install with: pip install -r requirements.txt

# Core data libraries
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0          # Parquet file support for pandas

# F1 data sources
fastf1>=3.7.0            # F1 timing, results, weather (2018+)
requests>=2.31.0         # HTTP calls to OpenF1 API
ratelimit>=2.2.1         # Rate limiting for OpenF1 (3 req/s)

# Machine learning
lightgbm>=4.3.0          # Default gradient boosted trees model
xgboost>=2.0.0           # Alternative model (swappable via config)
scikit-learn>=1.5.0      # Preprocessing, validation, metrics

# Optimization
PuLP>=2.8.0              # Integer linear programming for team selection
optuna>=3.5.0            # Bayesian hyperparameter tuning

# Statistics
scipy>=1.13.0            # Bootstrap confidence intervals, Spearman correlation

# Configuration
PyYAML>=6.0.1            # Parse config.yaml

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0
streamlit>=1.40.0
opencv-python-headless>=4.10.0
rapidocr-onnxruntime>=1.4.3

# NOTE:
# Keep this file runtime-focused for Streamlit deployment stability.
# Dev-only tools (Jupyter, test runners) live in requirements-dev.txt.
